type ActionType = {
    type: string
}

const initialState = {}

export const forgotReducer = (state = initialState, action: ActionType) => {
    switch (action.type) {
        default:
            return state
    }
};